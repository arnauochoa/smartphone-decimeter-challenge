% Script description

clearvars -except
close all; clc;

[project_path, ~, ~] = fileparts(mfilename('fullpath'));
[workspace_path, ~, ~] = fileparts(project_path);

%% Load configuration
[CONFIG] = loadConfig();

%% Input


%% Compute geometry


%% Navigate


%% Output

